<!DOCTYPE html>
<html lang="en-us">

<head>
    <title>file hosting</title>

    <meta name="robots" content="noindex, follow">
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, user-scalable=1, initial-scale=1.0">
    <meta name="robots" content="noindex, follow">
    <meta property="og:site_name" content="CoolElectronics.me gif hosting">
    <meta property="og:url" content="<%= content %>">
    <meta property="og:title" content="<%= name %>">
    <meta property="og:description" content="<%= description%>">

    <meta property="og:type" content="video.other">
    <meta property="og:image" content="<%= content %>">
    <meta property="og:image:type" content="image/gif">
    <!-- <meta property="og:image:url" content="<%= content %>">
    <meta property="og:image:secure_url" content="<%= content %>"> -->
    <!-- <meta property="og:image:type" content="image/gif"> -->
    <!-- <meta property="og:image:width" content="1280"> -->
    <!-- <meta property="og:image:height" content="720"> -->
</head>

<body>
    <!-- <div id="page-container"> -->
    <div class="con fw" id="main-container">
        <div class="downloadbox con c2">
            <p class="m-text h2">
                <%= name %>
            </p>
            <br>
            <img src="<%= content %>" alt="">
            <br>
            <br>
            <%=description%>
        </div>
    </div>
    <!-- </div> -->
</body>

<style>
    .nopad {
        margin: 0px !important;
        padding: 0px !important;
    }

    :root {
        /* --m-font: Assistant; */
        --m-main-fg: #d9d9d9;
        --m-alt-fg: #bfbfbf;
        --m-alt-alt-fg: #a6a6a6;
        --m-inner-gaps-px: 5px;
        --m-font-fg: #0d0d0d;
        --m-gaps-px: 10px;
        --m-container-transparency: 94%;
        --m-container-radius: 10px;
    }

    .fa {
        width: 4%;
        cursor: pointer;
        height: auto;
        color: var(--m-font-fg);
    }

    html,
    body {
        width: 100%;
        height: 100%;
        overflow: hidden;
        background-color: var(--m-main-fg);
    }

    #main-container {
        width: calc(100% - var(--m-inner-gaps-px)*4);
        height: calc(100% - var(--m-inner-gaps-px)*4);
        background-color: var(--m-alt-fg);
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .c2 {
        background-color: var(--m-alt-alt-fg);
        /* display: flex; */
        border: 10px solid var(--m-alt-alt-fg);
    }

    .con {
        /* background-color: var(--m-alt-fg); */
        position: relative;
        border-radius: var(--m-container-radius);
        /* border: 2px solid var(--m-main-fg); */
        /* background-color: var(--m-main-fg); */
    }
</style>

</html>